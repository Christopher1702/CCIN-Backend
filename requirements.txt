# CCIN Backend — Python dependencies
# Layer 1 (descriptive stats) uses stdlib only — no third-party packages required.
# The packages below are included for Layers 2–6 and the future API layer.

# ── Data & analytics ──────────────────────────────────────────────────────────
duckdb>=0.10.0          # analytical database — stores snapshots and gap targets
numpy>=1.26.0           # numerical computing — Layer 2 trend calculations
scipy>=1.12.0           # STL decomposition, statistical tests — Layer 2/3

# ── HTTP / scraping (ingest pipeline) ────────────────────────────────────────
httpx>=0.27.0           # async HTTP client — NRCan scraper
beautifulsoup4>=4.12.0  # HTML parsing — NRCan page scraper

# ── Future API layer (uncomment when ready) ───────────────────────────────────
# fastapi>=0.111.0
# uvicorn[standard]>=0.29.0
